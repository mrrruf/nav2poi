# Navigate to POI

This simple and small app adds a valuable feature to Hammerhead Karoo 3 devices that former Garmin users might be missing: The ability to navigate to a global POI.

Global POIs can be saved on the Hammerhead Routes Dashboard. This app simply displays them as a list (ordered by distance) and lets the user pick one to navigate to.
